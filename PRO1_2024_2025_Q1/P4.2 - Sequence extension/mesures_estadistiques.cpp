#include <iostream>

using namespace std;

int main()
{
    cout.setf(ios::fixed);
    cout.precision(4);
    int a;
    double min, max, avg;

    cin >> a;

    for (int i = 0; i < a; i++)
    {
        int b;
        cin >> b;

        for (int j = 0; j < b; j++)
        {
        }
    }
}